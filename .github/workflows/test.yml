name: Play Ground
on:
  push:
    tags: '*'
  workflow_dispatch:

jobs:
  publish:
    name: Look around
    runs-on: ubuntu-latest

    steps:
      - name: Print environment
        run: |
          printenv | LC_ALL=C sort
          pwd -P
